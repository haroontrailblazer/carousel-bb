"""Fetcher package - pulls news into the queue and can start pipeline runs.

Kept empty (docstring only) to keep imports acyclic; import the module
directly: ``from fetcher import fetch_news`` / ``python -m fetcher.fetch_news``.
"""
