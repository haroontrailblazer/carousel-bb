# Carousel visual references

- `carousel-system-concept.png` is the coordinated design-system board derived
  from the seven supplied carousel sets and current Baskaran Builds web tokens.
  Cards 01-06 use the official website favicon. The final CTA card keeps the
  main Baskaran portrait and uses one favicon beside the handle in its rail.
  All seven cards use one fixed top-left number position and type size.
- `body-slide-proof.png` is a 1080 x 1350 visual QA proof for the data-evidence
  archetype.
- `baskaranbuilds-favicon.png` is the official 512 px favicon served by
  `baskaranbuilds.com`, used without redrawing it.
- `instagram-profile-source.png` is the user-supplied identity source retained
  as the main CTA portrait reference, never as a second footer icon.

Both images use fictional demonstration copy and numbers. They are visual
references only, never factual sources or publishable carousel content. Runtime
slides must use only the approved news item and verified research brief.
